# homebrew-tap

Official Homebrew tap for installing ClipGate CLI from prebuilt binaries.

## Install

```bash
brew install clipgate/tap/cg
```

Release binaries are hosted on [clipgate.github.io](https://clipgate.github.io/releases/).
